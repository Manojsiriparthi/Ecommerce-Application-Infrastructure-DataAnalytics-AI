'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {API,apiFetch} from '../../lib/api';export default function Orders(){const[o,setO]=useState<any[]>([]);useEffect(()=>{apiFetch(`${API.order}/api/orders`).then(r=>r.json()).then(d=>setO(d.orders||[]))},[]);return <main className="container"><h1>My Orders</h1>{o.map(x=><div className="card" key={x.id}><Link href={`/orders/${x.id}`}>{x.id}</Link><p>{x.deliveryStatus} · ₹{Number(x.totalAmount).toFixed(2)}</p></div>)}</main>}

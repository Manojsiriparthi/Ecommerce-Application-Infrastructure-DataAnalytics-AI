'use client';import {useEffect,useState} from 'react';import {useParams} from 'next/navigation';import {API,apiFetch} from '../../../lib/api';
export default function Order(){const {id}=useParams<{id:string}>();const [o,setO]=useState<any>();useEffect(()=>{apiFetch(`${API.order}/api/orders/${id}`).then(r=>r.json()).then(d=>setO(d.order))},[id]);if(!o)return <main className="container"><p>Loading...</p></main>;return <main className="container"><h1>Order Successful</h1><div className="card"><p><b>Order ID:</b> {o.id}</p><p><b>Payment:</b> {o.paymentTransactionId}</p><p><b>Order Status:</b> {o.status}</p><p><b>Delivery:</b> {o.deliveryStatus}</p><p><b>Total:</b> ₹{Number(o.totalAmount).toFixed(2)}</p><h3>Delivery Details</h3><pre>{JSON.stringify(o.addressJson,null,2)}</pre></div></main>}

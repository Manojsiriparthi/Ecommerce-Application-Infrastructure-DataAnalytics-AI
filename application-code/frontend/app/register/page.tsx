'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {API} from '../../lib/api';
export default function Register(){const [f,setF]=useState({name:'',email:'',phone:'',password:''}),[error,setError]=useState('');const r=useRouter();function u(k:string,v:string){setF({...f,[k]:v})}async function submit(e:FormEvent){e.preventDefault();const x=await fetch(`${API.user}/api/users/register`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const d=await x.json();if(!x.ok)return setError(d.message||'Registration failed');r.push('/login')}return <main className="container"><h1>Register</h1><form onSubmit={submit} className="card">{(['name','email','phone','password'] as const).map(k=><input key={k} className="input" placeholder={k} type={k==='password'?'password':'text'} value={f[k]} onChange={e=>u(k,e.target.value)} required/>)}{error&&<p className="error">{error}</p>}<button className="btn">Create Account</button></form></main>}

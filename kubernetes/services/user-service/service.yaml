apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: ecommerce
  labels:
    app: user-service
spec:
  type: ClusterIP
  selector:
    app: user-service
    tier: backend
  ports:
  - name: http
    port: 3001
    targetPort: 3001
    protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
    tier: backend
  ports:
  - name: http
    port: 3004
    targetPort: 3004
    protocol: TCP
